# 🚨 คำแนะนำสำคัญ: การเลือก Exchange

> [!IMPORTANT]
> **ปัญหาที่พบบ่อย:** Binance TH API ไม่ได้รับการรองรับอย่างเต็มที่จาก CCXT

## ✅ แนะนำสำหรับผู้ใช้งาน

### 1. **Binance Global** (แนะนำที่สุด ⭐)
```env
EXCHANGE=binance
# ไม่ต้องใส่ API Key ถ้าแค่ดึงข้อมูล
```

**ข้อดี:**
- ✅ CCXT รองรับเต็มที่
- ✅ ดึงข้อมูลได้แบบ Public API (ไม่ต้อง API Key)
- ✅ ราคาเหมือน Binance TH (ตลาดเดียวกัน)
- ✅ มี Liquidity สูงสุด

**ข้อจำกัด:**
- ❌ ถ้าจะเทรด ต้องมีบัญชี Binance Global (ไม่ใช่ TH)

---

### 2. **Binance Testnet** (แนะนำสำหรับทดสอบ 🧪)
```env
EXCHANGE=binance_testnet
USE_TESTNET=true
API_KEY=vmPUZE6mv9SD5VNHk4HlWFsOr6aKE2zvsw0MuIgwCIPy6utIco14y7Ju91duEh8A
API_SECRET=NhqPtmdSJYdKjVHjA7PZj4Mge3R5YNiP1e3UZjInClVN65XAbvqqM6A7H5fATj0j
```

**ข้อดี:**
- ✅ ทดสอบฟรี ไม่ใช้เงินจริง
- ✅ รับเหรียญทดสอบฟรี
- ✅ ทดสอบการเทรดได้จริง
- ✅ ไม่ต้อง KYC

**ข้อจำกัด:**
- ❌ ข้อมูลอาจไม่เหมือนตลาดจริง 100%

**วิธีสมัคร:**
1. ไปที่ https://testnet.binance.vision/
2. Login ด้วย GitHub
3. Generate HMAC_SHA256 Key
4. ไปที่ Faucet รับเหรียญฟรี

---

### 3. **Binance Thailand** (ไม่แนะนำสำหรับ Bot ⚠️)
```env
EXCHANGE=binanceth
# จะถูก Redirect ไปใช้ Binance Global แทน
```

**ปัญหา:**
- ❌ CCXT ไม่รองรับ Binance TH API โดยตรง
- ❌ API Endpoint แตกต่างจาก Binance Global
- ❌ ใช้งานได้ยาก มี Error บ่อย

**ทางเลือก:**
- ✅ ใช้ **Binance Global** แทน (ดึงข้อมูลได้เหมือนกัน)
- ✅ เทรดผ่าน Web ของ Binance TH แทน
- ✅ ใช้ Bot กับ Binance Global, ดูราคาบน Binance TH

---

## 🎯 คำแนะนำตามวัตถุประสงค์

| ต้องการ | แนะนำ Exchange | วิธีตั้งค่า |
|---------|---------------|------------|
| **ดึงข้อมูลราคา** | Binance Global | `EXCHANGE=binance` (ไม่ต้อง API Key) |
| **ทดสอบ Bot** | Binance Testnet | `EXCHANGE=binance_testnet` + API Key |
| **เทรดจริง (ไทย)** | Binance Global | สมัครที่ binance.com |
| **เทรดจริง (ต่างประเทศ)** | Binance Global | สมัครที่ binance.com |

---

## 💡 FAQ

### Q: ทำไม Binance TH ใช้ไม่ได้?
**A:** CCXT (library ที่ใช้) ไม่ได้พัฒนา support สำหรับ Binance TH โดยเฉพาะ เพราะ Binance TH เป็น regional exchange ที่มี API แยกต่างหาก

### Q: ราคาจาก Binance Global กับ Binance TH ต่างกันไหม?
**A:** ราคาเกือบเหมือนกัน เพราะเป็นตลาดเดียวกัน อาจต่างกันนิดหน่อยเพราะ Spread และ Liquidity

### Q: ถ้าอยากเทรดบน Binance TH ทำไง?
**A:** มี 2 ทาง:
1. ใช้ Bot บน Binance Global แทน (แนะนำ)
2. เขียน Custom API Client เอง (ยาก ไม่แนะนำ)

### Q: Testnet ต่างจาก Demo Trading ยังไง?
**A:**
- **Testnet (testnet.binance.vision)** = มี API ใช้กับ Bot ได้ ✅
- **Demo Trading (demo.binance.com)** = เล่นบน Web เท่านั้น ❌

---

## 🔧 ตัวอย่างการใช้งานที่ถูกต้อง

### ดึงข้อมูลแบบ Public API (ไม่ต้อง API Key)

```python
from data_handler import DataHandler

# ใช้ Binance Global
data_handler = DataHandler(use_public_only=True)

# ดึงข้อมูล
df = data_handler.fetch_ohlcv('BTC/USDT', '1h', 100)
price = data_handler.get_current_price('BTC/USDT')

print(f"ราคา BTC: ${price:,.2f}")
```

### ทดสอบบน Testnet

```python
# ตั้งค่าใน .env
EXCHANGE=binance_testnet
USE_TESTNET=true
API_KEY=<your_testnet_key>
API_SECRET=<your_testnet_secret>

# รันโค้ด
python main.py
```

---

**อัปเดต:** 2026-01-08  
**สถานะ:** Binance TH ไม่แนะนำสำหรับ Bot, ใช้ Binance Global แทน
