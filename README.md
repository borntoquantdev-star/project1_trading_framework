# 🤖 Trading Bot Framework (Project 1)

โปรเจคนี้แสดงการสร้าง **Trading Bot แบบมืออาชีพ** ที่มีโครงสร้างแข็งแรง ปลอดภัย และขยายต่อได้ง่าย ตามหลักการ **4-Block System**

## 📚 เนื้อหาที่ครอบคลุม

- ✅ โครงสร้างแบบ Modular (แยกไฟล์ตามหน้าที่)
- ✅ ความปลอดภัยของ API Keys (ใช้ .env)
- ✅ Data Handler (ดึงข้อมูลผ่าน REST API)
- ✅ Strategy Module (Setup-Trigger-Filter Model)
- ✅ Execution Engine (ยิง Order แบบปลอดภัย + Idempotency)
- ✅ Safety Net (Error Handling, Retry Logic)
- ✅ Line Notify Integration
- ✅ Dry Run Mode (ทดสอบโดยไม่เสียเงิน)

## 🏗️ โครงสร้างโปรเจค

```
project1_trading_framework/
├── .env.example          # ตัวอย่างการตั้งค่า API Keys
├── .gitignore            # ป้องกัน API Keys หลุด
├── config.py             # ตั้งค่าบอท (คู่เหรียญ, Timeframe, Risk)
├── main.py               # จุดเริ่มต้นการทำงาน (4-Block System)
├── data_handler.py       # Block 1: ดึงข้อมูลตลาด
├── strategy.py           # Block 2: สมองกลยุทธ์
├── execution.py          # Block 3: ยิงคำสั่งซื้อขาย
├── utils.py              # Block 4: เครื่องมือเสริม (Line, Logger)
├── requirements.txt      # รายการ Library ที่ต้องใช้
└── README.md             # เอกสารนี้
```

## 🚀 การติดตั้ง (Setup)

### 1. ติดตั้ง Python Libraries

```bash
pip install -r requirements.txt
```

### 2. สร้าง API Keys จาก Exchange

> [!WARNING]
> **สำคัญ!** Binance TH API ไม่ได้รับการรองรับเต็มที่จาก CCXT  
> **แนะนำ:** ใช้ **Binance Global** (binance.com) แทน - ราคาเหมือนกัน ใช้งานได้ดีกว่า

#### Binance Global (แนะนำ ⭐)
1. สมัครที่ https://www.binance.com/
2. ยืนยันตัวตน (KYC) 
3. สร้าง API Keys ที่ https://www.binance.com/en/my/settings/api-management
   - ✅ เปิด: `Enable Spot Trading`
   - ❌ ปิด: `Enable Withdraw` (เพื่อความปลอดภัย!)

**ไม่ต้อง API Key ถ้าแค่ดึงข้อมูล:**
```env
EXCHANGE=binance
# ไม่ต้องใส่ API_KEY และ API_SECRET
DRY_RUN=true
```

#### Binance Thailand (สำหรับคนไทย)
1. สมัครที่ https://www.binance.th/
2. ยืนยันตัวตน (KYC)
3. สร้าง API Keys ที่ https://www.binance.th/th/my/settings/api-management
4. ตั้งค่าใน `.env`: `EXCHANGE=binanceth`

#### Binance Testnet (ทดสอบฟรี ไม่ใช้เงินจริง!)

> [!IMPORTANT]
> **ห้ามสับสน!**
> - ❌ `demo.binance.com` = Paper Trading บน Web (ไม่ใช่ API)
> - ✅ `testnet.binance.vision` = API Testnet (ใช้กับ Bot ได้)

**วิธีใช้งาน:**
1. ไปที่ **https://testnet.binance.vision/** (ไม่ใช่ demo.binance.com)
2. Login ด้วย GitHub
3. สร้าง API Keys (Generate HMAC_SHA256 Key)
4. รับเหรียญทดสอบฟรีจาก Faucet
5. ตั้งค่าใน `.env`:
   ```
   EXCHANGE=binance_testnet
   USE_TESTNET=true
   API_KEY=<คัดลอกจาก testnet.binance.vision>
   API_SECRET=<คัดลอกจาก testnet.binance.vision>
   ```

**ตัวอย่าง API Key:** `vmPUZE6mv9SD5VNHk4HlWFsOr6aKE2zvsw0MuIgwCIPy6utIco14y7Ju91duEh8A`

#### OKX / Bybit
- เหมือน Binance แต่ใช้ `EXCHANGE=okx` หรือ `EXCHANGE=bybit`

### 3. ตั้งค่าไฟล์ .env

```bash
# คัดลอกไฟล์ตัวอย่าง
copy .env.example .env

# จากนั้นแก้ไขไฟล์ .env ใส่ API Keys ของคุณ
```

ตัวอย่าง `.env` สำหรับ **Binance Global**:
```env
EXCHANGE=binance
API_KEY=your_real_api_key_here
API_SECRET=your_real_api_secret_here
LINE_NOTIFY_TOKEN=your_line_token_here  # (Optional)
DRY_RUN=true  # เซ็ตเป็น false เมื่อพร้อมเทรดจริง
```

ตัวอย่าง `.env` สำหรับ **Binance Thailand**:
```env
EXCHANGE=binanceth
API_KEY=your_binance_th_api_key
API_SECRET=your_binance_th_api_secret
DRY_RUN=true
```

ตัวอย่าง `.env` สำหรับ **Binance Testnet** (แนะนำสำหรับมือใหม่):
```env
EXCHANGE=binance_testnet
USE_TESTNET=true
API_KEY=your_testnet_api_key
API_SECRET=your_testnet_api_secret
DRY_RUN=false  # Testnet ปลอดภัย ใช้เงินปลอม
```

### 4. (Optional) ตั้งค่า Line Notify

1. ไปที่ https://notify-bot.line.me/
2. สร้าง Token ใหม่
3. ใส่ใน `.env`

## ▶️ วิธีใช้งาน

### ทดสอบแต่ละโมดูล

```bash
# ทดสอบ Config
python config.py

# ทดสอบ Data Handler
python data_handler.py

# ทดสอบ Strategy
python strategy.py

# ทดสอบ Execution
python execution.py
```

### รัน Bot

```bash
python main.py
```

เมื่อรันจะมีให้เลือก 2 โหมด:
- **โหมด 1**: รัน 1 รอบเท่านั้น (เหมาะสำหรับทดสอบ)
- **โหมด 2**: รันแบบต่อเนื่อง (เหมาะสำหรับ Production)

## 🔐 ความปลอดภัย (Security)

### ⚠️ **กฎเหล็ก:**

1. **ห้าม Hardcode API Keys** ลงในโค้ดเด็ดขาด
2. **ห้ามให้ API Key มีสิทธิ์ Withdraw**
3. **ห้าม Commit ไฟล์ .env** ขึ้น Git (มี .gitignore ป้องกันไว้แล้ว)
4. **ใช้ Dry Run Mode** ทดสอบก่อนเสมอ

### ตรวจสอบความปลอดภัย

```bash
# ตรวจสอบว่าไฟล์ .env ถูก ignore หรือยัง
git status

# ต้องไม่เห็น .env ใน list
```

## 📋 Go-Live Checklist

ก่อนปล่อยบอททำงานจริง ตรวจสอบ:

- [ ] เงินในบัญชีมีพอสำหรับขั้นต่ำ (Binance ขั้นต่ำ ~$10)
- [ ] API Key Permission ถูกต้อง (Trade: Yes, Withdraw: No)
- [ ] ลองรัน Dry Run Mode แล้ว (DRY_RUN=true)
- [ ] ตรวจสอบ Timezone ของเครื่อง/Server
- [ ] ติดตั้ง Line Notify แล้ว (เพื่อรับแจ้งเตือน)
- [ ] อ่านและเข้าใจ Code ทั้งหมดแล้ว

## 🎓 หลักการทำงาน (4-Block System)

### Block 1: Data Handler (ดวงตา)
- ดึงข้อมูล OHLCV ผ่าน REST API (CCXT)
- มี Rate Limiting เพื่อป้องกันโดน Ban
- มี Retry Logic กรณีเน็ตหลุด

### Block 2: Strategy (สมอง)
- ใช้โมเดล **Setup-Trigger-Filter**
- ตัวอย่างกลยุทธ์: RSI + EMA
- ออกแบบแบบ Modular เปลี่ยนได้ง่าย

### Block 3: Execution Engine (มือ)
- ยิง Market/Limit Order
- มี **Idempotency Check** (ไม่ซื้อซ้ำ)
- ตรวจสอบยอดเงินก่อนยิง Order

### Block 4: Safety Net (เกราะป้องกัน)
- Try-Except ครอบทุกจุดเสี่ยง
- Retry Logic สำหรับ Network Error
- Line Notify แจ้งเตือนทุกเหตุการณ์สำคัญ

## 🛠️ การปรับแต่ง (Customization)

### เปลี่ยนคู่เหรียญ
แก้ไขใน `config.py`:
```python
SYMBOL = 'ETH/USDT'  # เปลี่ยนจาก BTC/USDT เป็น ETH/USDT
```

### เปลี่ยน Timeframe
```python
TIMEFRAME = '1h'  # เปลี่ยนจาก 15m เป็น 1h
```

### ปรับขนาด Position
```python
POSITION_SIZE_USDT = 50  # เพิ่มจาก 20 เป็น 50 USDT
```

### เปลี่ยนกลยุทธ์
แก้ไขในไฟล์ `strategy.py` หรือสร้างคลาสใหม่:
```python
# ใช้กลยุทธ์ EMA Crossover แทน
from strategy import EMACrossoverStrategy
self.strategy = EMACrossoverStrategy()
```

## 🧪 Dry Run Mode

โหมดนี้จะ**จำลอง**การเทรดโดยไม่ใช้เงินจริง:
- ดึงข้อมูลตลาดจริง
- คำนวณสัญญาณจริง
- แต่**ไม่ยิง Order จริง** (แค่บันทึก Log)
- เหมาะสำหรับทดสอบ Logic

เปิดโหมดนี้ด้วยการตั้ง `DRY_RUN=true` ใน `.env`

## 📊 การอ่าน Log

Log จะบอกคุณว่าเกิดอะไรขึ้น:

```
✅ = สำเร็จ
❌ = ล้มเหลว/Error
⚠️ = คำเตือน
🚀 = กำลังยิง Order
💰 = ข้อมูลเงิน/ราคา
📊 = ข้อมูลสัญญาณ
🧪 = Dry Run Mode
```

## 🚦 Deployment (ขั้นตอนอัพขึ้น Server)

### 1. ทดสอบบนเครื่องตัวเองก่อน
```bash
python main.py  # เลือกโหมด 1 (รัน 1 รอบ)
```

### 2. เช่า VPS (Cloud Server)
แนะนำ:
- DigitalOcean
- AWS EC2 (Free Tier)
- Google Cloud

### 3. ติดตั้ง Environment บน Server
```bash
# อัพเดต Python
sudo apt update
sudo apt install python3 python3-pip

# Clone Code (ห้ามเอา .env ขึ้น!)
git clone your-repo
cd project1_trading_framework

# ติดตั้ง Dependencies
pip3 install -r requirements.txt

# สร้าง .env บน Server ใหม่
nano .env  # แล้วใส่ API Keys
```

### 4. รันด้วย PM2 หรือ Screen (เพื่อให้รันต่อเนื่อง)
```bash
# ติดตั้ง Screen
sudo apt install screen

# สร้าง Screen Session
screen -S tradingbot

# รัน Bot
python3 main.py

# กด Ctrl+A แล้ว D เพื่อ Detach
# Bot จะทำงานต่อใน Background
```

## 🐛 Troubleshooting

### เกิด ImportError
```bash
pip install -r requirements.txt --upgrade
```

### API Key ไม่ถูกต้อง
- ตรวจสอบว่าคัดลอก API Key/Secret ถูกต้อง
- ตรวจสอบว่า Permission ถูกต้อง (Trade: Yes)

### Network Error บ่อยเกินไป
- เช็คอินเทอร์เน็ต
- Exchange อาจมีปัญหา ลองรอสักครู่

### ยอดเงินไม่พอ
- เช็คว่ามีเงินใน Spot Wallet มากกว่า `POSITION_SIZE_USDT`
- Exchange อาจมีขั้นต่ำสูงกว่าที่ตั้ง

## 📚 แหล่งเรียนรู้เพิ่มเติม

- **CCXT Documentation**: https://docs.ccxt.com/
- **Binance API**: https://binance-docs.github.io/apidocs/spot/en/
- **Line Notify**: https://notify-bot.line.me/doc/

## ⚖️ คำเตือน (Disclaimer)

- การเทรดมีความเสี่ยง อาจสูญเสียเงินลงทุนได้
- โปรเจคนี้เป็นเพียงตัวอย่างเพื่อการศึกษา
- ผู้พัฒนาไม่รับผิดชอบต่อความเสียหายใดๆ
- **ใช้เงินทุนที่พร้อมเสียได้ 100% เท่านั้น**

## 📧 การสนับสนุน

หากพบปัญหาหรือมีคำถาม:
1. ตรวจสอบ Log ดูรายละเอียด Error
2. ตรวจสอบ Config ว่าถูกต้องหรือไม่
3. อ่าน Error Message อย่างละเอียด

---

**สร้างด้วย ❤️ สำหรับการเรียนรู้ Algorithmic Trading**
